# Test and lint tooling. Kept out of requirements.txt so Render's runtime image
# doesn't ship a test runner and a static analyzer.
-r requirements.txt

pytest>=8.0,<9
# tests/test_github_issues.py uses @pytest.mark.asyncio. Without this plugin the
# markers are unknown and the coroutines never actually execute — the tests look
# like they pass while asserting nothing.
pytest-asyncio>=0.23
# Enforces the billing seam contracts in .importlinter (INV-8).
import-linter>=2.0
