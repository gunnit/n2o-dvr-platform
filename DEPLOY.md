# Deployment — N2O DVR Platform

Fully on **Render.com** (Frankfurt). Blueprint at `backend/render.yaml` declares 5 resources:

| Resource | Name | Type | Notes |
|---|---|---|---|
| Managed Postgres | `n2o-dvr-db` | `database` | `basic-256mb` plan (legacy `starter` was retired for new DBs) |
| Redis | `n2o-dvr-redis` | `redis` | `maxmemoryPolicy: noeviction`, `ipAllowList: []` (internal only) |
| Web service | `n2o-dvr-api` | `web` (python) | FastAPI + uvicorn, 1GB disk mounted at `/data` |
| Worker service | `n2o-dvr-worker` | `worker` (python) | Celery, 1GB disk mounted at `/data` |
| Web service | `n2o-dvr-frontend` | `web` (node) | Next.js 16, Node 20 |

## Live URLs

| Service | URL |
|---|---|
| Frontend (canonical) | https://dvr-sicurezza.it |
| Frontend (www) | https://www.dvr-sicurezza.it |
| Frontend (Render fallback) | https://n2o-dvr-frontend.onrender.com |
| API | https://n2o-dvr-api.onrender.com |
| API Swagger | https://n2o-dvr-api.onrender.com/docs |

The custom domain `dvr-sicurezza.it` is registered at Register.it. DNS:
- Apex `dvr-sicurezza.it` → A record `216.24.57.1` (Render's static IP for apex domains)
- `www.dvr-sicurezza.it` → CNAME `n2o-dvr-frontend.onrender.com.`
- Email records (MX, SPF, autoconfig, autodiscover, pec) untouched — keep on Register's mail infra.
- SSL via Let's Encrypt, auto-renewed by Render.

## 1. First-deploy runbook

1. `dashboard.render.com/select-repo?type=blueprint` → pick `gunnit/n2o-dvr-platform` branch `main` → blueprint path `backend/render.yaml`.
2. Render prompts for `OPENAI_API_KEY` (marked `sync: false` on api + worker). Paste it.
3. Apply. Render provisions all 5 resources + wires `DATABASE_URL` and `REDIS_URL` via `fromDatabase` / `fromService` automatically.
4. **Optional:** upload Google Drive OAuth token on `n2o-dvr-worker`:
   - Service → **Environment** → **Secret Files**
   - Filename: `token.json`
   - Contents: paste `credentials/token.json` from the local repo
   - `GOOGLE_DRIVE_TOKEN_JSON` already points at `/etc/secrets/token.json`; without this file, Drive uploads silently no-op (doc generation still succeeds).
5. `alembic upgrade head` runs automatically via `preDeployCommand`.

## 2. Environment reference

### `n2o-dvr-api` (FastAPI)

| Var | Source |
|---|---|
| `DATABASE_URL` | Auto from `n2o-dvr-db` (postgres://…) — normalized to `postgresql+asyncpg://` in code |
| `REDIS_URL` | Auto from `n2o-dvr-redis` |
| `AUTH_SECRET` | Auto-generated by blueprint |
| `OPENAI_API_KEY` | **Set manually** |
| `OPENAI_ORG_ID` | **Set manually** (optional; only if org-scoped billing) |
| `CORS_ORIGINS` | `["https://dvr-sicurezza.it","https://www.dvr-sicurezza.it","https://n2o-dvr-frontend.onrender.com"]` |
| `FILE_STORAGE_PATH` | `/data` (persistent disk) |

### `n2o-dvr-worker` (Celery)

Same as API plus:
| Var | Value |
|---|---|
| `GOOGLE_DRIVE_FOLDER_ID` | `13aHCy8D78JwJzgffxYbqe7Nmyed84may` (N2O's production folder) |
| `GOOGLE_DRIVE_TOKEN_JSON` | `/etc/secrets/token.json` (Secret File) |

### `n2o-dvr-frontend` (Next.js)

| Var | Value |
|---|---|
| `NODE_VERSION` | `20` |
| `NEXT_PUBLIC_API_URL` | `https://n2o-dvr-api.onrender.com` (baked at build time) |
| `AUTH_SECRET` | Auto-generated (independent from backend's — NextAuth encrypts cookies, backend signs its own JWTs) |
| `NEXTAUTH_URL` | `https://dvr-sicurezza.it` (canonical custom domain) |

## 3. Render quirks (discovered during first deploy — all already encoded in `render.yaml`)

These bit us the first time; don't undo them:

| Quirk | Why | Fix in `render.yaml` |
|---|---|---|
| Postgres `plan: starter` is rejected | Render retired the legacy starter plan for new DBs | `plan: basic-256mb` |
| Redis needs explicit `ipAllowList` | Blueprint validator requires it | `ipAllowList: []` (internal-only) |
| Default region is Oregon | N2O client is Italian, data stays in EU | `region: frankfurt` on every resource |
| Build runs from repo root, not `backend/` | `cp -r ../templates ./templates` fails otherwise | `rootDir: backend` on api + worker |
| `alembic` can't find `app` module | Alembic doesn't add CWD to `sys.path` | `preDeployCommand: PYTHONPATH=. alembic upgrade head` |
| Alembic tries psycopg2, not asyncpg | SQLAlchemy maps `postgres://` to the default sync driver | `backend/alembic/env.py` calls `_normalize_async_url()` (same helper as `session.py`) |
| Auth.js v5 errors `UntrustedHost` | Render's reverse proxy rewrites Host; NextAuth v5 refuses it for open-redirect protection | Set `AUTH_TRUST_HOST=true` on `n2o-dvr-frontend` |
| Next.js 16 build fails: `useSearchParams() should be wrapped in a suspense boundary` | Strict prerender rule in app router | Wrap the consuming component in `<Suspense>` (see `frontend/src/app/(auth)/login/page.tsx`) |

## 4. Known gaps (acceptable for v1, track for follow-up)

| Gap | Impact | Workaround |
|---|---|---|
| **US-1.6 signature feature is half-built.** Migration `c1d2e3f4a5b6_add_azienda_signature.py` adds 3 columns to `aziende`, but the backend model, schemas, and `/sign` `/signature` `/revision` routes don't exist. The frontend "Conferma firma" button at `survey-wizard.tsx:324` posts to a 404. | Signature step in the survey wizard is broken. | Skip the signature step during the real demo; the columns sit unused in Postgres. |
| **Self-registration creates a new organization with the registrant as admin.** This is standard SaaS flow (admin of *their own* org), not a privilege escalation — organizations are tenant-scoped. | By design. | To invite operators into an existing org, use `backend/scripts/seed_demo_users.py` as a template. |
| **NextAuth ↔ backend JWT refresh.** There's no refresh flow; the backend JWT TTL is 7 days (`ACCESS_TOKEN_EXPIRE_MINUTES` in `backend/app/config.py`). NextAuth's own session TTL is 30 days. After 7 days of activity the user is bounced back to `/login`. | User has to log in once a week. | Acceptable for v1. |
| **SDS extraction runs inline on FastAPI** (`backend/app/api/v1/sostanze_chimiche.py:172`, `BackgroundTasks`), not on Celery. | Concurrent users may see slower API responses during batch SDS uploads. | Small teams won't notice. |
| **Unused `backend/Dockerfile`.** The Render blueprint uses `runtime: python`, not Docker. | None. | Delete or fix alongside a future Docker migration. |

## 5. Troubleshooting

| Symptom | Likely cause |
|---|---|
| Build fails: `cp: cannot stat '../templates'` | `rootDir: backend` missing. See §3. |
| Pre-deploy fails: `ModuleNotFoundError: No module named 'app'` | `PYTHONPATH=.` missing on `preDeployCommand`. See §3. |
| Pre-deploy fails: `No module named 'psycopg2'` | `env.py` not using `_normalize_async_url()`. See §3. |
| Pre-deploy fails: `Multiple head revisions are present for given argument 'head'` | Parallel migration branches. Run `PYTHONPATH=. alembic merge -m "merge" <head1> <head2>` locally + commit. |
| Next.js build fails: `useSearchParams() should be wrapped in a suspense boundary` | Any page reading `useSearchParams` must be wrapped in `<Suspense>` (client component). Currently only `/login` does this — fixed in `frontend/src/app/(auth)/login/page.tsx`. |
| Frontend returns 502 for ~2 min after deploy | Normal Render cold-start; uvicorn/next is binding the port. |
| Generated .docx is blank / missing cover page | `templates/` didn't get copied during build. Check API build log. |
| "Conferma firma" returns 404 | Expected — US-1.6 signature feature not implemented. See §4. |
| Drive uploads log "Google Drive token.json not found" | Secret File not uploaded. See §1 step 4. |

---

*Last updated 2026-04-17 — after first successful full-stack deploy to Frankfurt.*
