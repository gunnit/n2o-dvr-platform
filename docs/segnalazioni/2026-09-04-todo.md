# To-do — 2026-09-04 (after the Luca follow-up call)

Sources: live `GET /api/v1/feedback` on the N2O tenant (158 rows: 122 risolto,
30 nuovo, 3 in_revisione, 3 non_fara; 0 new since 2026-09-02), GitHub issues
(newest still #76, 2026-06-10), Render logs (api service, Aug 26 → Sep 4),
Gemini notes of today's "Follow up" with Luca (15:00), the Sep 4 meeting prep,
and the Render billing thread with Elena (Sep 3). Previous triage:
`2026-09-02-triage.md`. The clarification email drafted on Sep 2 was never
sent; today's call superseded most of it.

## A. This week — infrastructure and hygiene

1. **Render card.** "Invalid payment info" (Sep 3): every service, including
   n2o-dvr-api/worker/frontend/db/redis, is suspended if the retry fails.
   Elena (Sep 3, 14:16) proposes putting the Bebit card on now and moving to
   the Unicredit prepaid card in ~15 days; she asked "facciamo insieme?" and
   has no answer yet. Reply and update https://dashboard.render.com/billing.
2. **Serper credits.** Still `400` on every lookup — including 15:23–15:24
   CEST today, i.e. during the call with Luca (two P.IVA lookups ran with
   `facts_keys=0, serper=0`). This is segnalazione "compilazione automatica
   imprecisa" (Aug 19) and the meeting's "imprecisione inserimento dati
   aziendali". Renew serper.dev; decide on `OPENAPI_API_KEY`.
3. **Rotate `GITHUB_TOKEN`** on n2o-dvr-api, then
   `python -m scripts.backfill_feedback_issues --apply --since 2026-06-10`
   (89 rows unmirrored; dry-run first).
4. **Mark the shipped Aug 3–5 batch `risolto`** in /admin/feedback — 20 rows
   remain (the PEE focus bug was already flipped; 122 vs 121 on Sep 2). Two
   need Luca's word first: "Aree valutazione dei rischi completamente da
   rivedere" (umbrella?) and the POS default diciture.
5. **Schedule next week's review call** with Luca (agreed today; not in the
   calendar). Ask Andrea for the annotated DVR (yellow highlights).
6. **Send Luca a short recap** of today's decisions + the open questions in
   section C, replacing the unsent Sep 2 draft.

## B. Build — decided today

| # | Item | Notes |
|---|---|---|
| 7 | Carica Visura at the start of the censimento, next to P.IVA autofill | Endpoint and extractor exist (`POST /aziende/{id}/visura`, 15 credits, `visura_extractor.py`); today only reachable after creation (Panoramica tab). Needs the upload in `aziende/new` — either create-then-upload or a pre-create flow. |
| 8 | Table of risks excluded by the AI suggestion, with "ripristina" | Segnalazione Aug 19 (catalogo). No existing code path for excluded risks. |
| 9 | Per-row flag "come da documenti allegati" on the risk-index table | Segnalazione Aug 19 (allegati voce per voce). Decide what happens to existing category-level links. |
| 10 | Photo extraction for Incendio + PEE per-ambiente fields | Descrizione/metratura/materiali + sorgenti di innesco from photos (vision, 4 credits); numero persone stays manual. AI proposes, operator confirms. Segnalazioni Aug 25 ×2. |
| 11 | Misure di miglioramento: dedupe + prompt tuning | Root cause: `misure_miglioramento.py` dedupes by `pericolo_valutazione_id`, not by text. Meeting also asks for prompt accuracy/neutrality. Segnalazione Aug 21. |
| 12 | DVR layout fixes | Wait for Andrea's annotated DVR. Design system shipped Sep 3–4 (`6327879`, `e0875fa`, `edf1d16`) — re-verify on the canary azienda after the fixes. |
| 13 | Credit estimate for the new AI buttons | Weights: reasoning 1, vision 4, sds 8, visura 15. N2O usage so far 2,779 / 9,000 (sds 2,144, reasoning 314, visura 225, vision 96). Deliverable for Luca/Simone. |
| 14 | Automatic monthly e-invoicing for instalment plans (ROX Pay) | Verify the procedure. Product/admin decision, not code yet; PayPal in prod is still sandbox. |

## C. Still waiting on N2O (not covered in today's notes)

15. OCR carta d'identità / permesso di soggiorno — GDPR sign-off (consent
    owner, retention of the image, data controller). In revisione since Apr 28.
16. Lavorazioni di default POS — need N2O's list (GH #63).
17. Guida with new screenshots — keep the structure or reorganize? (GH #36).
18. Gestanti AI button — may it conclude "non compatibile" or only suggest?
19. Protocollo sanitario AI + disease list — input basis (INAIL tables?).
20. Rischio Biologico: 5 new categories — need Word sources; originals are
    the unparseable ones (two `.doc`, one PDF).
21. "Ho finito i crediti ma li ho" (Jul 31) — not reproducible; 6,221 credits
    left. Ask for date/time or close.

## D. Commercial / admin from the prep, not visible in the notes

22. VERA final price, Luca's provvigioni from year 2, pilot banca written
    acceptance (demo Jun 25, 2.5k€), fattura 236 reconciliation with Emanuela
    (Aug 7). Confirm what was decided today; the notes only mention
    rateizzazione and recurring subscriptions.

## Parked

- GH #4 versioning scheda cliente, GH #1 catalogo attrezzature riutilizzabile
  (Apr 19 enhancements, no client pressure).
- 3 `non_fara` rows stay as they are.
